'use strict';

let Ops = {
    checkDuplicate: function(arr, val) {
        let counter = 0;
        for(let element of arr) {
            if(element === val) {
                counter++;
                continue;
            } else {
                continue;
            }
        }
        return counter;
    },

    randInt: function(int, max) {
        
    },
}